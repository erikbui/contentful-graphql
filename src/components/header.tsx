import { css } from "../../styled-system/css";

export const Header = () => (
  <div
    className={css({
      display: "flex",
      bg: "#8acf01",
      h: "4rem",
      justifyContent: "center",
      alignItems: "center",
    })}
  >
    Header
  </div>
);
